SELECT CASE
WHEN G.GRADE < 8 THEN NULL
ELSE S.NAME
END AS STUDENT_NAME, G.GRADE, S.MARKS
FROM STUDENTS S
JOIN GRADES G
ON S.MARKS<=G.MAX_MARK AND G.MIN_MARK<=S.MARKS
ORDER BY G.GRADE DESC, STUDENT_NAME, S.MARKS ASC;
